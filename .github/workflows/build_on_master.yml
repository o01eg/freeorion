name: "Build master"
on:
  push:
    branches:
      - master
      - 'release-v*'
jobs:
  windows:
    uses: ./.github/workflows/_build-windows.yml
  windows-msvc:
    uses: ./.github/workflows/_build-windows-msvs.yml
